#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Printf is used to format
 * and print text to standard output
 * allows the control of formatting by using format specifiers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char string[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", string);

	return (0);

}
