#include "main.h"

/**
 * main - test  function that prints if integer is + or -
 * we are testing when given a case study of zero 0
 *
 * Return: 0
 */

int main(void)
{
	int i;

	/* assign 0 to i */
	i = 0;
	positive_or_negative(i);

	return (0);
}
